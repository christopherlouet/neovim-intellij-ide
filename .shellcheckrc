# ShellCheck configuration for neovim-intellij-ide
# https://github.com/koalaman/shellcheck/wiki/Directive

# Follow sourced files (e.g., /etc/os-release)
external-sources=true

# Shell dialect
shell=bash

# Disabled checks with justification:
# SC2034 - Variable appears unused (intentional in some install patterns)
# SC2015 - A && B || C is not if-then-else (intentional pattern for success/warn messages)
# SC2016 - Expressions don't expand in single quotes (intentional for run() function)
disable=SC2034,SC2015,SC2016
