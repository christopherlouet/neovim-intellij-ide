# Release v2.3.0 - Theme Selector

## Highlights

- ✨ **Sélecteur de thèmes** avec 7 colorschemes (`<leader>ut`)
- 🎨 Persistance du choix entre sessions
- 🐛 Corrections de bugs (database.lua, matrix plugin)

## What's New

### Theme Selector

Nouveau sélecteur de thèmes accessible via `<leader>ut` :

| Thème | Description |
|-------|-------------|
| Tokyo Night | Clean dark theme (défaut) |
| Dracula | Dark theme with vibrant colors |
| Catppuccin | Soothing pastel theme |
| Nord | Arctic, north-bluish palette |
| Gruvbox | Retro warm colors |
| Cyberpunk | Neon cyberpunk aesthetic |
| Matrix | Matrix-inspired green terminal |

Le thème choisi est automatiquement sauvegardé et restauré au prochain lancement.

### Bug Fixes

- Fix du plugin matrix (remplacé par `iruzo/matrix-nvim`)
- Fix de l'erreur nil dans `database.lua` pour nvim-cmp
- Fix de la restauration du thème au démarrage

## Installation / Mise à jour

```bash
# Mise à jour
cd ~/.config/nvim
git pull

# Synchroniser les plugins
nvim --headless "+Lazy! sync" +qa

# Ou relancer Neovim et faire :Lazy sync
```

## Full Changelog

**Commits:**

- `fa33865` feat(ui): add theme selector with 7 colorschemes
- `778244c` fix(ui): use valid matrix-nvim plugin from iruzo
- `ed3e21d` docs: add theme selector documentation
- `ef170dc` fix(ui): fix theme persistence and database.lua nil error

**Compare:** <https://github.com/christopherlouet/neovim-intellij-ide/compare/v2.2.0...v2.3.0>
